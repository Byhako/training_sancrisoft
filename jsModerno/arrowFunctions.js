// fn declaration
function add (x, y) {
  return x + y
}

// fn expression
let add = function (x, y) {
  return x + y
}

// arrow function
let add = (x, y) => {
  return x + y
}

let add = (x, y) => x + y

lista.map(function () {
  // lo que sea
})

lista.map(() => {
  // lo que sea
})
